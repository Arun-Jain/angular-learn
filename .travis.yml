sudo: false
language: node_js
node_js:
  - node

addons:
apt:
  sources:
    - google-chrome
  packages:
    - google-chrome-stable

before_script:
- npm install

script: 
- ng test

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    npm run build;
    fi
